from hopital.h import get_main # из пакета хопитал в H вызвать функию
from hopital.patients.index import get_index

get_main() 
get_index()

# если запутался, тебе нужно открыть такие файлы как:
# main, h, surgeons, nersus, index (5 файлов)